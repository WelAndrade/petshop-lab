function searchProduct() {
    // Redireciona para a página de busca sem parâmetros
    window.location.href = 'busca.html';
    
    // Impede o comportamento padrão de recarregar a página ao submeter o formulário
    return false;
}
